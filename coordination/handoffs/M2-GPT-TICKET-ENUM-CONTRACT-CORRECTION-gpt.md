# Handoff — M2-GPT-TICKET-ENUM-CONTRACT-CORRECTION

- Task: `M2-GPT-TICKET-ENUM-CONTRACT-CORRECTION`
- Branch: `ai/gpt/m2-ticket-enum-contract-correction`
- Base SHA: `4ac2d9a43b93acb34c0bb89d4908da7c52f7279c`
- Implementation SHA: `2473d2b4fe8c191bf468f8d4af4b7d45be5c7d5b`
- Head: implementation SHA plus the immediately following handoff-only commit

## Summary and files

Corrected the pre-plan Ticket enum values in `prisma/schema.prisma`, added corrective migration
`prisma/migrations/20260715193000_correct_ticket_enums/migration.sql`, bound
`src/contracts/operations.ts` to the generated Prisma priority enum, and added unit/database
catalog coverage in `tests/platform/ticket-enum-contract*.test.ts`.

The migration preserves legacy rows as follows: FASILITAS -> SARANA, LAYANAN/KEUANGAN ->
LAINNYA, NORMAL -> SEDANG, DARURAT -> URGENT, and DITUTUP -> DITOLAK. It adds
KEMAHASISWAAN and DIVERIFIKASI. The original merged migration was not edited.

## Impact

- Schema/API contract: Ticket categories, priorities, statuses, and the priority default now
  match docs 02/14. New default is SEDANG.
- Migration: one forward-only PostgreSQL corrective migration; no destructive row deletion.
- Dependencies, environment, routes, UI, seed: unchanged.
- Generated Prisma output is intentionally ignored by Git and is regenerated by CI.

## Acceptance

| Command | Result |
|---|---|
| corrective `npx prisma migrate deploy` on the existing PostgreSQL test database | PASS |
| `npm run prisma:generate` | PASS |
| `npm run prisma:validate` | PASS |
| `npm run lint` | PASS |
| `npm run typecheck` | PASS |
| `npm test` | PASS — 347 passed, 43 DB-gated skipped |
| `npm run test:integration` | PASS — 43 passed, including exact PostgreSQL enum catalog/default checks |
| `npm run build` | PASS |
| `npm audit --audit-level=high` | PASS — 0 High/Critical; 5 Moderate transitive findings |
| `git diff --check` | PASS |

## Risks and follow-ups

- CI must prove both migrations from an empty PostgreSQL 17 database; local verification
  proved the corrective migration on the existing PostgreSQL 16 test database.
- Ticket workflow/routes remain M4. This task intentionally corrected only frozen enum data.
