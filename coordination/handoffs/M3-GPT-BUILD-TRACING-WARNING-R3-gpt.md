# Handoff — M3-GPT-BUILD-TRACING-WARNING-R3

- Task: `M3-GPT-BUILD-TRACING-WARNING-R3`
- Branch: `ai/gpt/m3-build-tracing-warning-r3`
- Frozen assignment: `6aed1d4ae27d8be3de599541a916e380e4c09e2a`
- Implementation: `b3429351c2fef81da15a935e45f6e61877a40720`
- Superseded, never-merge candidate:
  `5535c1c44f4b758f27b318b8d501482507bdc06f`

## Summary

Reimplemented the standalone tracing correction after a prospectively committed manifest and active
lease. Runtime-configured storage paths carry narrowly placed `turbopackIgnore` annotations; no
storage behavior, contract, schema, dependency, root configuration, or API changed.

The R3 source diff has stable patch ID
`c187ab86a9dbf94b17d44630b94a2c41af438752`, exactly matching the independently reviewed source diff
from the superseded candidate. The unleased candidate itself was not cherry-picked or merged.

## Files changed

- `src/lib/storage/staged-file.ts`
- `coordination/handoffs/M3-GPT-BUILD-TRACING-WARNING-R3-gpt.md`

## Verification

All mandatory code gates passed on fresh GPT-owned PostgreSQL state:

| Command/evidence | Result |
| --- | --- |
| `npm run prisma:validate` | PASS |
| `npx prisma migrate deploy` | PASS — 2 migrations from empty database |
| `npm run prisma:seed` | PASS |
| `npm run lint` | PASS |
| `npm run typecheck` | PASS |
| `RUN_PLATFORM_DB_TESTS=true npm test` | PASS — 49 files, 738 tests |
| `npm run test:integration` | PASS — 20 files, 83 tests |
| `npm run build` | PASS — compiled successfully, zero Turbopack warnings |
| `git diff --check` | PASS |
| R3 scope check | PASS — 1 source file within lease before this handoff |

The full static/unit/integration/build run used
`fuspi_test_gpt_m3_buildr3_20260729111235`, which was dropped after execution.

### NFT evidence

`.next/server/app/api/admin/media/route.js.nft.json` contains 235 files. The only repository source
files are these seven required storage runtime modules:

- `src/lib/storage/committed-file.ts`
- `src/lib/storage/error.ts`
- `src/lib/storage/index.ts`
- `src/lib/storage/paths.ts`
- `src/lib/storage/ppks-attachment.ts`
- `src/lib/storage/staged-file.ts`
- `src/lib/storage/validate-upload.ts`

There are zero coordination, docs, tests, E2E, Prisma, or source files outside the storage runtime
domain in that NFT.

### Authenticated standalone smoke

The production standalone artifact ran internally at `127.0.0.1:3117`, with a temporary TLS
loopback terminator at `https://127.0.0.1:3118` so the production `Secure` session cookie could be
issued and replayed naturally. A freshly seeded admin fixture was marked as having completed its
mandatory initial password change before login.

| Operation | Result |
| --- | --- |
| real `POST /api/auth/credentials` login | HTTP 200 |
| authenticated `GET /api/admin/media` | HTTP 200 |
| authenticated `POST /api/admin/media/upload` | HTTP 200 |
| authenticated `POST /api/admin/media` delete | HTTP 200 |

The session token was generated by the credentials endpoint; it was not forged or inserted manually.
The smoke used database `fuspi_test_gpt_m3_buildr3_tls_20260729112649` and storage under
`/tmp/fuspi-gpt/m3-build-r3/`; the database was dropped after the run.

## Harness diagnostics

Several fail-closed harness attempts preceded the final passing smoke:

1. an initial NFT assertion rejected all seven legitimate storage source modules; classification was
   narrowed only after listing and verifying every path;
2. ImageMagick `convert` was unavailable, so the PNG fixture was generated with the installed
   project `sharp` dependency;
3. port 3101 was briefly occupied, so the isolated smoke moved to 3117/3118;
4. HTTP curl correctly withheld the production `Secure` cookie, so the final run used local TLS;
5. the seeded admin initially returned `SESSION_INVALID` at Media transport because
   `mustChangePassword=true`; the final fixture represented a completed initial-password flow.

Every database from these attempts was dropped. None exposed a product or tracing defect, and no
source was changed in response.

## Risk and follow-up

- VPS filesystem permissions and real reverse-proxy TLS remain deployment gates, not M3 local gates.
- A future Next.js upgrade must replay the zero-warning build, NFT classification, and standalone
  Media smoke before removing or changing the tracing directives.
- The final combined M3 gate still must run after this candidate is merged.
